# LitNavigator — optional live-AI config.
#
# To enable REAL LLM teaching:
#   1) rename this file to ".env"   (exactly .env — no .txt)
#   2) paste your OpenAI API key after LITNAV_LLM_API_KEY=
#   3) run LitNavigator.exe
#
# Without this file, LitNavigator runs fully OFFLINE (deterministic, $0, no key) —
# the complete agentic flow still works.

LITNAV_LLM_PROVIDER=openai
LITNAV_LLM_API_KEY=sk-your-openai-key-here
LITNAV_LLM_MODEL=gpt-4o-mini
LITNAV_EMBED_MODEL=text-embedding-3-small
